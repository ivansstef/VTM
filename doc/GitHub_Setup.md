# GitHub Setup Instructions for VTM Project

## 🚀 1. Repository Setup

### Local Repository
```bash
# Ініціалізація локального репозиторію (вже виконано)
git init

# Перевірка статусу
git status

# Додавання файлів з виключенням документації
git add .

# Початковий коміт
git commit -m "Initial VTM MVP commit"

# Перейменування основної гілки в main (якщо потрібно)
git branch -M main
```

### Remote Repository Setup
```bash
# Додавання віддаленого репозиторію
git remote add origin https://github.com/ivansstef/VTM.git

# Перевірка підключення
git remote -v

# Відправка коду на GitHub
git push -u origin main
```

## 🔒 2. Branch Protection Rules

Налаштування на GitHub:

1. Перейти в **Settings** -> **Branches**
2. Додати правило для гілки `main`:
   - ✓ Require pull request reviews before merging
   - ✓ Require status checks to pass before merging
   - ✓ Include administrators
   - ✓ Require linear history
   - ✓ Require signed commits

## 👥 3. Collaboration Guidelines

### Branches
- `main` - стабільна версія
- `develop` - розробка
- `feature/*` - нові функції
- `fix/*` - виправлення помилок
- `release/*` - підготовка релізу

### Commit Messages
```bash
# Формат
type(scope): description

# Типи
feat:     нова функціональність
fix:      виправлення помилок
docs:     зміни в документації
style:    форматування коду
refactor: реорганізація коду
test:     додавання тестів
chore:    обслуговування
```

## 📝 4. Pull Request Template

```markdown
## Опис змін
<!-- Що було зроблено? -->

## Тип змін
- [ ] Bug fix
- [ ] New feature
- [ ] Breaking change
- [ ] Documentation update

## Чек-лист
- [ ] Тести додано/оновлено
- [ ] Документацію оновлено
- [ ] Код відповідає стандартам
- [ ] Всі тести проходять

## Додаткова інформація
<!-- Будь-які додаткові коментарі -->
```

## 🔄 5. Git Workflow

### Початок роботи над функцією
```bash
git checkout -b feature/name
```

### Щоденні операції
```bash
# Оновлення локального репозиторію
git pull origin develop

# Додавання змін
git add .
git commit -m "feat: add new feature"

# Відправка змін
git push origin feature/name
```

### Завершення роботи
```bash
# Оновлення з develop
git checkout develop
git pull origin develop
git checkout feature/name
git rebase develop

# Виправлення конфліктів (якщо є)
git add .
git rebase --continue

# Відправка змін
git push origin feature/name --force
```

## 🛡️ 6. Security Best Practices

1. Не комітити конфіденційні дані:
   - Паролі
   - Ключі API
   - Локальні налаштування
   
2. Використовувати `.env` для конфігурації:
   ```bash
   # .env
   SECRET_KEY=your_secret_key
   DATABASE_URL=sqlite:///path/to/db
   ```

3. Регулярно оновлювати залежності:
   ```bash
   pip freeze > requirements.txt
   ```

## 📦 7. Release Process

1. Створення релізної гілки:
   ```bash
   git checkout -b release/v1.0.0
   ```

2. Оновлення версії та документації

3. Фінальне тестування

4. Злиття в main:
   ```bash
   git checkout main
   git merge release/v1.0.0
   git tag -a v1.0.0 -m "Version 1.0.0"
   git push origin main --tags
   ```

## 🔍 8. Code Review Guidelines

1. Перевірка коду:
   - Відповідність стандартам
   - Наявність тестів
   - Документація оновлена
   
2. Коментарі:
   - Конструктивні
   - З прикладами
   - З посиланнями на документацію

3. Процес:
   - Перегляд змін
   - Запуск тестів
   - Перевірка локально
   - Залишення коментарів
   - Апрув або реквест змін
